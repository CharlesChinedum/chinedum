---
import { navIcons, navLinks } from "../data";
---

<nav class="flex justify-between py-3 px-10">
  <div>
    <p class="text-2xl text-gray-200 font-bold">Chinedum.</p>
  </div>
  <!-- <div class="flex gap-3">
    {
      navIcons.map((item) => (
        <img
          src={`https://cdn.simpleicons.org/${item}`}
          alt=""
          class="w-5 grayscale hover:grayscale-0"
        />
        //  hover:scale-110 transform transition duration-300 ease-in-out
      ))
    }
  </div> -->
  <div class="flex gap-4 text-xl">
    {
      navLinks.map((item) => (
        <p class="cursor-pointer hover:font-bold  text-gray-400 hover:text-[#1f75fe]">
          {item}
        </p>
      ))
    }
  </div>
</nav>
